name:Python Test

on: [push]

jobs:
  test:
    run-on: ubuntu-latest

  steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: set up Python
      uses: actions/setup-python@v5
      with:
	python-version: "3.10"
    - name:Run test
      run:python main.py

    
        